uses math;
var
  number: real;
begin
  writeln('Задача. Написать модуль, содержащий тригонометрическую функцию секанс sec');
  writeln;
  write('Введите число -> ');
  readln(number);
  writeln('Результат выполнения функции: ', sec(number));
end.  